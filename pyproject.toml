[project]
name = "good-template2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pre-commit>=4.3.0",
    "ruff>=0.13.1",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.13.1",
]
