[project]
name = "good-template2"
version = "0.1.0"
description = "a template for a future project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "pre-commit>=4.3.0",
    "ruff>=0.13.1",
]

[tool.uv]
dev-dependencies = [
]
